# 台账操作与业务单据：本地预览检查点

日期：2026-09-13
仓库：/Users/federal/Documents/Codex/2026-09-12/new-chat-3/work/github-integration-20260913/web
分支：stage/2026-09-13-next-development
前一检查点：5d489fc。本阶段只修改 web，未部署、未推送。

## 已落实

- 产情、市场共用操作栏：查看记录、编辑，以及有文字标签的“更多操作”。原有淘汰与删除处理器、确认与权限条件保留。操作栏统一 260px；其他通用列顺序及尺寸、蓝色表头保留。
- 已选正式样本直接进入填写，不再显示无关查询。嵌入式样本选择流程保留。
- 对象资料、栏目定位、连续分组表单、固定保存区。业务数据在前，地理定位在后；长对象名称自然换行。
- 我的任务进入正式样本填写时不重复显示外层标题、业务类型切换条；返回后恢复原有入口。
- 新建/原业务详情由 920px 小弹层改为占满工作区的单据；分组卡片改为连续单据分节。保留原路由、接口与权限。
- 独立单据不再依赖历史列表加载，也不在保存成功后额外读取隐藏列表。此前列表请求失败会通过 Promise.all 阻断对象类型与表单配置；实页新建曾空白，本次修复后产情、市场均加载真实字段。
- 新建提交文案不再声称进入人工审核；底层提交接口及真实 review 模式未改。

## 验证

- 相关测试累计 95 项通过：正式样本填写 48、业务单据 19、产情 12、市场 16。
- 新增回归：历史列表接口不可用时，独立新建仍加载配置并允许填写；确认不请求无关列表。
- 原有保存载荷、原子位置保存、未保存编辑保护、权限与操作处理器等相关测试通过。
- npm run build、修改源文件 ESLint、git diff --check 通过。
- 真实登录本地预览 https://localhost:29445：产情/市场新建真实字段、启用的保存按钮、无错误；产情/市场正式样本填写、无重复查询、栏目定位；更多操作内删除/淘汰按钮完整可见。
- 没有为视觉验证修改、淘汰或删除真实业务数据。真实持久化写入本阶段未执行；依赖现有回归验证保存行为。

## 未完成，不能宣称整套改造完成

- 用户尚未认可整体视觉质量；本阶段效果只供真实预览评审。
- 左侧目录对应的组织授权、样本管理、历史样本、分析、地区填报等仍须分别落实统一的信息组织与业务操作。
- 新建地区选择仍为原有逐级查询/选择控件，尚未重构成整合检索组件。
- 列表排序的业务规则尚未另作改动；本阶段保护已有原列顺序与字段宽度。
- 历史列表接口自身失败的服务端根因未进一步定位；已解除其对独立新建的错误依赖，不应声称后台问题全数解决。
- MyTasksWorkspace 既有权限映射逻辑本阶段未改；下一阶段如调整组织/角色，须按真实后端授权验证。

## 继续规则

保护现有工作树；不重复云端会话修复或整套 CI，不以截图代替真实功能。
先评审本阶段真实单据与操作效果，再按业务页面类别推进一阶段；不因设计而删除字段、伪造状态或增加重复页面。
UI 尚未获部署确认。不得据此发布。
