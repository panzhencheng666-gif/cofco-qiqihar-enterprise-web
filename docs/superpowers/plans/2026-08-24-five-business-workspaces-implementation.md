# 五个业务内容区实施计划

> 当前分支：`feature/20260823-sample-network-comparison`

## 完成条件

五页真实功能全部保留，统一工作台样式落地；范围外页面无源码变更；Node24 测试与构建通过；受管运行副本同步、哈希、健康、浏览器和 Nginx 验收通过；功能分支完成提交和推送并可创建 PR。

## 实施顺序

1. 记录工作树、目标文件和现有定向测试基线。
2. 先写结构契约测试，再调整待办和已办内容区。
3. 先写结构契约测试，再调整导入任务内容区。
4. 先写结构契约测试，再调整样本点管理内容区。
5. 先写结构契约测试，再调整账号与权限内容区。
6. 运行五页定向回归并审计文件范围。
7. 使用 Node24 运行项目门禁和生产构建。
8. 同步受管运行副本，执行 SHA-256、8090/63182/63200 健康及浏览器验收。
9. 验证现有 Nginx 发布链，复核后提交并推送功能分支，准备 PR。

## 风险控制

- 仅在现有真实组件中增加语义容器和样式，不重写数据层。
- 不触碰总揽监测及产情、市场、物流填报组件。
- 不复制数据库，不改生产、云端或 `main`。
- 长命令无输出时主动读取进程状态，不重复已经通过的门禁。
