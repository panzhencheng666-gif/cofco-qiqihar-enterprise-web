# 人员与权限真实预览

阶段：stage/2026-09-13-next-development。仅改 Web 账号模块，暂不部署公网。

已确认设计：保留企业工作台，个人中心放头像入口；管理区展示员工管理、地区分工、授权检查、操作日志。单位作为筛选条件。员工编辑使用右侧抽屉；地区分工复用原有责任接口和编辑器。保留真实邀请、安全服务、复核及日志功能，禁止虚构数据与权限。

实施：

1. 调整 IdentityGovernancePanel 导航、地区列表与抽屉，添加模块专用样式。
2. 保留所有后端写入和权限判断；单位旧入口重定向管理区，普通用户回个人中心。
3. 更新受影响的导航测试，验证地区分工调用原编辑器；执行模块测试及构建。
4. 启动独立 HTTPS 本地预览，连接现有真实登录后端。浏览器验证只读流程，记录登录或接口限制；不进行业务测试写入。

完成条件：可打开真实前端预览，明确实际数据来源与已验证范围，留下提交或交接检查点。公网部署等待用户验收。

## 检查点

- 已实现四个管理入口、单位筛选、员工设置抽屉、按已分配地区筛选员工；保留原有写接口和权限判断。
- 通过：账号与责任编辑器测试 31 项，工作台入口定向测试 1 项，TypeScript、Vite 构建、改动文件 ESLint。
- 本地 HTTPS 预览 29445 连接现有 OIDC 后端 28090，不注入测试身份。登录经既有 29443 回调，登录后返回 29445 查看新版。
- 待用户登录后验收真实数据与操作，尚未验证保存到真实后端；无公网部署、无合并。
- 预览启动脚本与本地登录模板修复备份位于当前 Codex 任务的 work/；登录模板的 client.baseUrl 空值导致 500，已做本地单行修复并验证正常显示登录表单。该运行时修复不属于本 Web 提交。
